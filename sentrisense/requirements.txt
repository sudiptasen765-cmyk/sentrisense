# =========================================================
# SentriSense — Python dependencies
# Split conceptually: [ML / training] vs [backend / Lambda-local]
# Lambda deployment packages use their own per-function
# requirements.txt (backend/lambda/<fn>/requirements.txt)
# to keep deployed package size minimal.
# =========================================================

# ---- Core ----
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.1
joblib==1.4.2
scipy==1.13.1

# ---- NLP ----
nltk==3.8.1
regex==2024.5.15
rake-nltk==1.0.6

# ---- Transformer experimentation (Phase 4/5 only — NOT deployed to Lambda) ----
torch==2.3.1
transformers==4.42.4
datasets==2.20.0

# ---- Evaluation / plotting ----
matplotlib==3.9.1
seaborn==0.13.2

# ---- Local backend (FastAPI dev server mirroring Lambda handlers) ----
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.8.2

# ---- AWS ----
boto3==1.34.144
botocore==1.34.144

# ---- Testing ----
pytest==8.2.2
pytest-cov==5.0.0
httpx==0.27.0
moto==5.0.11

# ---- Dev tooling ----
python-dotenv==1.0.1
black==24.4.2
