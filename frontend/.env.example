# Base URL of the SentriSense API Gateway endpoint.
# Defaults to the live production endpoint if unset (see src/services/api.js).
# Override this only if testing against a different stage/environment.
VITE_API_BASE_URL=https://av9stuspv9.execute-api.ap-south-1.amazonaws.com
